enum RequestState { idel, loading, success, fail }

//request method
enum RequestMethod { post, patch, delete }

enum AGState { idel, camera, loading, result }

enum ChatAI { connecting, connected, failure, closed, idel, thinking }

enum ChatStatus { sent, received, read, delivered, deleted }
